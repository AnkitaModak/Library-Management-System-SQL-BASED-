--Retrieve the title of book B001 using SELECT ... INTO and display it.
DECLARE
   VK_BOOKTITLE BOOKS.BOOKTITLE%TYPE;
BEGIN
   SELECT BOOKTITLE
   INTO VK_BOOKTITLE
   FROM BOOKS
   WHERE BOOKID = 'B001';

   DBMS_OUTPUT.PUT_LINE(VK_BOOKTITLE);
END;
/
